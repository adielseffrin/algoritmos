
public class Main {

	public static void main(String[] args) {
		Elemento inicio, fim, aux;
		
		Elemento novo = new Elemento();
		novo.numero = 10;
		
		inicio = novo;
		fim = novo;
		
		novo = new Elemento();
		novo.numero = 15;
		
		aux = inicio;
		while(aux.prox != null && aux.prox.numero < novo.numero)
			aux = aux.prox;


		if(aux.prox == null) {
			//to no final
			aux.prox = novo;
			fim = novo; // = aux.prox;
		}else {
			//to no meio
			novo.prox = aux.prox;
			aux.prox = novo;
		}
		
		
		novo = new Elemento();
		novo.numero = 11;
		
		aux = inicio;
		while(aux.prox != null && aux.prox.numero < novo.numero)
			aux = aux.prox;


		if(aux.prox == null) {
			//to no final
			aux.prox = novo;
			fim = novo; // = aux.prox;
		}else {
			//to no meio
			novo.prox = aux.prox;
			aux.prox = novo;
		}
		
		
		
		novo = new Elemento();
		novo.numero = 120;
		
		aux = inicio;
		while(aux.prox != null && aux.prox.numero < novo.numero)
			aux = aux.prox;


		if(aux.prox == null) {
			//to no final
			aux.prox = novo;
			fim = novo; // = aux.prox;
		}else {
			//to no meio
			novo.prox = aux.prox;
			aux.prox = novo;
		}
		
		
		aux = inicio;
		while(aux != null) {
			System.out.println(aux.numero);
			aux = aux.prox;
		}
		
		
	}

}
